using DeMobile.Hubs;
using DeMobile.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace DeMobile.Controllers
{
    public class ChatController : ApiController
    {
        //public IHttpActionResult PostSendMessage([FromBody]SendMessage value)
        //{
        //    ChatHub hub = new ChatHub();
        //    if (value.username == "%")
        //        hub.SendMessageAll(value.message);
        //    else
        //        hub.SendMessage(value.message, value.username);
        //    return Json(new { result = "sent" });
        //}
    }
}
